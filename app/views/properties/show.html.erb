<div class="block rounded-lg p-4 shadow-sm shadow-indigo-100">
  
  
  <% if @property.images.attached? %>
    <% @property.images.each do |i| %>
      <%= image_tag i, class: 'h-56 w-full rounded-md object-cover' %>
    <% end %>
  <% else %>
    <%= image_tag 'full_pic_interior_apartment.avif', class: 'h-56 w-full rounded-md object-cover' %>
  <% end %>

  <div class="mt-2">
    <dl>
      <div>
        <dt class="sr-only">Price</dt>

        <dd class="text-sm text-gray-500">$<%= @property.price %>,00</dd>
      </div>

      <div>
        <dt class="sr-only">Address</dt>

        <dd class="font-medium"><%= @property.city %>, <%= @property.country %></dd>
      </div>
    </dl>

    <div class="mt-6 flex items-center gap-8 text-xs">
      <div class="sm:inline-flex sm:shrink-0 sm:items-center sm:gap-2">

        <div class="mt-1.5 sm:mt-0">
          <p class="text-gray-500">Parking</p>

          <p class="font-medium">2 spaces</p>
        </div>
      </div>

      <div class="sm:inline-flex sm:shrink-0 sm:items-center sm:gap-2">

        <div class="mt-1.5 sm:mt-0">
          <p class="text-gray-500"><%= @property.baths %> Bathrooms</p>
        </div>
      </div>

      <div class="sm:inline-flex sm:shrink-0 sm:items-center sm:gap-2">

        <div class="mt-1.5 sm:mt-0">
          <p class="text-gray-500">Bedroom</p>

          <p class="font-medium"><%= @property.bedrooms %> rooms</p>
        </div>
      </div>
    </div>
  </div>
</div>

<% if current_user.guest? %>
<div class="block rounded-lg p-4 shadow-sm shadow-indigo-100">
  <div class="mt-2">
    <dl>
      <div> 
        <dd class="font-medium"><%= link_to "Reserve", new_booking_path(id: @property.id) %></dd>
      </div>
    </dl>
  </div>
</div>

<% end %>

<% if current_user.owner? %>
<div class="block rounded-lg p-4 shadow-sm shadow-indigo-100">
  <div class="mt-2">
    <dl>
      <div>      
        <dd class="font-medium"><%= link_to "Show booking requests", bookings_path(property_id: @property.id) %></dd>
      </div>
    </dl>
  </div>
</div>

<% end %>