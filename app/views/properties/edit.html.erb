<%= render 'partials/section_title', title: 'Edit property' %>
<div class="mt-10 sm:mt-0 text-black">
  <div class="md:grid md:grid-cols-3 md:gap-6">
    <div class="mt-5 md:mt-0 md:col-span-2">
      <%= render "form" %>
    </div>
  </div>
</div>
